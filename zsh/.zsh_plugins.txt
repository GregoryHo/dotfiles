# Antidote bundle list. Loaded by `antidote load` in .zshrc.
# Order matters: completion providers -> fzf-tab -> syntax highlighter (last).

# OMZ-derived bundles. We cherry-pick directories instead of loading the whole
# framework; `kind:fpath` adds completions without sourcing init code.
ohmyzsh/ohmyzsh path:lib/git.zsh             kind:fpath
ohmyzsh/ohmyzsh path:plugins/git
ohmyzsh/ohmyzsh path:plugins/docker          kind:fpath
ohmyzsh/ohmyzsh path:plugins/kubectl
ohmyzsh/ohmyzsh path:plugins/react-native    kind:fpath

# Completion UI: must load AFTER compinit, BEFORE the syntax highlighter.
Aloxaf/fzf-tab

# Syntax highlighter LAST. `kind:defer` per upstream recommendation.
zdharma-continuum/fast-syntax-highlighting kind:defer
