# Antidote bundle list. Loaded by `antidote load` in .zshrc.
#
# Aliases are owned by the user (see ~/.zshrc.local) — no opinionated alias
# packs from upstream. OMZ-derived bundles here are completion providers only.

# fpath-only providers — cheap, load eagerly.
ohmyzsh/ohmyzsh path:plugins/docker          kind:fpath
ohmyzsh/ohmyzsh path:plugins/react-native    kind:fpath

# Completion UI — defer to first idle.
Aloxaf/fzf-tab                                kind:defer

# Syntax highlighter LAST. `kind:defer` per upstream recommendation.
zdharma-continuum/fast-syntax-highlighting kind:defer
